import React from "react";
import { styled } from "styled-components";

export default function Card() {
	return <CardEl>Card</CardEl>;
}

const CardEl = styled.div`
	border-radius: 1rem;
	background: #ffffff;
	padding: 1.5rem 2rem;
	width: 100%;
`;
